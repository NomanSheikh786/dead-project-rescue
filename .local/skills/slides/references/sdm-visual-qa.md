<sdm_visual_qa>
Run this pass after the deck builds, before the cover screenshot. The documents you just wrote are already in context, so most checks are read-backs of that YAML, not tool calls. Repair SDM documents and copy only; never add JSX wrappers or edit `.sdm/` runtime files.

1. Run `validate-slides`; every diagnostic (`text-overflow`, `text-overlap`, `theme-drift`, ...) is a build error. Fix only the named document path/element, then rerun. Never present a deck with outstanding issues.
2. Check the shared export scaffold: `/allslides` must render one 1920×1080 `.slide[data-slide-id]` wrapper per manifest slide; routing stays on wouter; `PARENT_OWNS_NAVIGATION` stays at module scope; the two scaffold navigation effects in `App.tsx` stay unchanged; and `slideLoader.ts` stays module-level. If `App.tsx`, `slideLoader.ts`, or `main.tsx` were hand-edited away from that shape, restore the scaffold before continuing or thumbnails/PDF/PPTX may fail with `NO_SLIDES_FOUND`.
3. Audit the documents you wrote -- these are read-backs, not screenshots:
   - **Motifs**: every decorative shape belongs to one of the deck's declared motifs and has a job -- framing content or an image, dividing, anchoring, or bleeding off an edge (`./sdm-design.md`). Delete floaters: a shape in open space or on a photo with no job is a defect, and clean whitespace beats filler. A shape-drawn chart is the same defect at any size, and so is a shape diagram past a small labeled flow of three to five boxes -- replace those with a widget, not a generated image. A shape collage imitating pictorial content becomes a generated image, and the small labeled flow itself stays as shapes (`./sdm-design.md`).
   - **Type scale**: at most 2 families and 3 weights deck-wide; body text stays 19pt+ and serif is used selectively. Titles are consistent per header style -- every slide using the same style shares one `sizePt` -- and a hero or statement title is its own deliberate style, not a one-off enlargement of the content title. Large titles are welcome when they are the visual idea; otherwise confirm they do not crowd or compete with the slide's image, stat, chart, or body.
   - **Grid**: content slides share the deck margin and content-top line, and every slide that uses a header style reuses that style's exact title frame and `sizePt`; elements meant to align share exact `x` or `y` values.
   - **Craft**: check for the avoidable failure modes in `## Designed, not templated` (`./sdm-design.md`): text plus rules where a meaningful visual would help, mechanically equal spacing or repeated stripes, mismatched photography, generic vector art where a photo fits, or related multiline/list copy mechanically split into one-line elements without an independent layout, style, action, or motion purpose. Sparse and typography-only slides are fine when intentional.
   - **Contrast**: every text-over-image element has a scrim shape between the image and the text in paint order.
   - **Density and copy**: max 6 text lines per slide; no emoji anywhere; no AI-slop vocabulary (`<constraints>` -- Content).
4. Bounded screenshot pass: screenshot ONE representative content slide (the busiest one) at its `/slideN` route, plus every slide that contains a `widget` -- `validate-slides` only verifies a widget's module file exists and never renders it, so a widget that fails at runtime shows red fallback text that would export literally, and these screenshots are the runtime check. A widget slide that is also the busiest slide needs only the one shot. Judge every shot -- squint test (does one element clearly dominate?), contrast test (is every word readable?), cohesion test (would it pass as the same deck as slide 1?) -- and on widget slides confirm the widget actually rendered: no red fallback text, no blank frame, labels legible. This pass takes no other screenshots: the `/slide1` cover shot belongs to `<first_build>` step 8, which runs right after this pass -- apply these same three tests to it there. Do not screenshot every slide, and never judge the whole deck from one `/allslides` screenshot -- the capped viewport shows only about 2 of its slides.
5. After any repair, rerun validation. If slide 1 or the deck theme changed, retake the `/slide1` cover screenshot. The pass ends only when the latest `validate-slides` run on the current files is error-free.
</sdm_visual_qa>

